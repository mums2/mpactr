# Limit Cores

Limits the amount of cores used to two. Mostly used in test and examples
so we can pass cran checks.

## Usage

``` r
limit_cores()
```

## Value

does not return anything.

## Examples

``` r
limit_cores()
```
